version: "3.2"
services:
  node:
    image: "node:8"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - /var/jenkins_home/workspace/nodejs/nodejs-training/:/home/node/app
    expose:
      - "8001"
    command: "npm start"